

## Mise à jour de l'accroche du Hero

L'utilisateur a choisi l'**Option B** avec une légère adaptation (« colis » au lieu de « conteneur »).

### Changement

Édition unique dans `src/lib/i18n.tsx`, clés `hero.title` et `hero.subtitle` :

- **Titre** : « Stop aux mauvaises surprises sur vos frais de fret. »
- **Sous-titre** : « Calculez le coût exact de votre colis depuis la Chine vers Lomé en 30 secondes. CBM, kg, délai d'arrivée — tout est là. »

### Fichier touché

- `src/lib/i18n.tsx` (clés `hero.title` + `hero.subtitle`, version FR)

Le reste du Hero (CTA, badge, animations, preview card) reste identique.

